// Shape B — the whole daymath surface, so the ceiling is visible next to the three-call floor.
import * as daymath from '../../../index.js'

console.log(Object.keys(daymath).length, daymath.addDays('2026-01-31', 30))
